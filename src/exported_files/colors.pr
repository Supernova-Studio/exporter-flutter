import 'package:flutter/material.dart';

class AppColors {
    
{[ const colorTokens = @ds.tokensByType("Color") /]}
{[ for token in colorTokens ]}
  {[ const tokenName = token.name.replacing("/", " ").camelcased(false) /]}
  {[ log token.description /]}
  {[ if @boolean.and(@compare.nonnull(token.description), @boolean.not(token.description.equals(""))) ]}
  /// {{ token.description }}
  {[/]}
  static const {{ tokenName }} = ;
{[/]}

  AppColors._();
}